<div class="container">
	<h1>Find friends</h1>
	<% @users.each do |user| %>
	 <%= user.user_name %>
	 <%= button_to("add friend", friendships_path(user_id: user.id), method: "post", class: "btn btn-small btn-inverse") %>
	<% end %>
</div>